start the heroku redis command each time before runserver

rm -rf .git ... great tool tp revert back the whole .git session

we will only see the messages which are there after we join

runtime.txt

gunicorn for wsgi
\

web: daphne chat_1.asgi:application --port $PORT --bind 0.0.0.0 -v2
worker: python manage.py runworker channels --settings=.mysite_1.settings -v2